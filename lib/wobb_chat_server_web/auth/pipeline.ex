defmodule ElixirChatbotInterfaceWeb.Auth.Pipeline do
  # use Guardian.Plug.Pipeline, otp_app: :elixir_chatbot_interface,
  #   module: ElixirChatbotInterfaceWeb.Auth.Guardian,
  #   error_handler: ElixirChatbotInterfaceWeb.Auth.ErrorHandler

  # plug Guardian.Plug.VerifyHeader
  # plug Guardian.Plug.EnsureAuthenticated
  # plug Guardian.Plug.LoadResource
end
